// Step 3: create folder reducer/kontak
const initialState = {

}

const kontak = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default kontak;